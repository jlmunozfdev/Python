{ 
    "builds": [{
        "src": "downloader/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "15mb", "runtime": "python.3.12" }
    }],
    "routers": [
        {
            "src": "/(.*)",
            "dest": "downloader/wsgi.py"
        }
    ]
}